[project]
name = "pywebview-vue3-template"
version = "0.1.0"
description = "Desktop application template: pywebview + Vue 3 + Tailwind CSS 4 + PrimeVue 4"
requires-python = ">=3.13"
dependencies = [
    "pywebview>=5.3",
]

[project.scripts]
app = "main:main"
