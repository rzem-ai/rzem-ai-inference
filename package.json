{
  "name": "rzem-ai-inference",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "description": "Desktop AI image generation app — Electrobun + Vue 3 + Python sidecar",
  "scripts": {
    "start": "cd frontend && npm run build && cd .. && electrobun dev",
    "dev": "electrobun dev --watch",
    "dev:hmr": "concurrently \"cd frontend && npm run dev\" \"electrobun dev\"",
    "build:canary": "cd frontend && npm run build && cd .. && electrobun build --env=canary"
  },
  "dependencies": {
    "electrobun": "^1.15.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "concurrently": "^9.2.1",
    "typescript": "^5.9.0"
  }
}
